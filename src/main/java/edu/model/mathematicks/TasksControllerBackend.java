package edu.model.mathematicks;

public class TasksControllerBackend {
    /* Тут мы принимаем запросы от TasksController и работаем с TaskDAO.
       При этом вся логика обработки задач будет в HandlerTask.
       Получаем задачу из TaskDAO, берем из нее все что запросил TasksController и отправляем обратно.
       Например, запросил id и condition, береи их из задачи и отправляем.
     */
}
